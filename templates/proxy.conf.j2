# {{ ansible_managed }}
{% for keys, values in awslogs_proxy_config_options_merged.items() %}
{%   if not values %}
{{     keys }}=
{%   else %}
{{     keys }}="{{ values }}"
{%   endif %}
{% endfor %}
